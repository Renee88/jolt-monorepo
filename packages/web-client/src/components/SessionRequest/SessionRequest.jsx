import React from 'react'
import type { RoomType, TalkType, UserType } from '../types'

const SessionRequest = ({jolter, talk, room}: {jolter: UserType, talk: TalkType, room: RoomType}) => {
  return (
    <div>
    
    </div>
  )
}

export default SessionRequest